## 1. Implementation
- [x] 引入 OpenCC WASM 並建立可重用的轉換器初始化（支援 s2t / t2s）
- [x] 建立搜尋索引文字（原文 + 轉換文）並更新比對/排序邏輯
- [x] 加入結果分頁狀態（visibleCount）與接近底部自動載入機制（例如 rootMargin 200px）
- [x] 更新鍵盤導覽：當選取到最後可見結果時自動擴展下一頁
- [x] 更新結果計數顯示為「已載入 / 總數」

## 2. Tests
- [x] 補強搜尋測試：簡繁互通匹配
- [x] 補強搜尋測試：滾動到底載入更多

## 3. Docs
- [x] 更新相關文件（若需要）並確認 spec delta 與實作一致
