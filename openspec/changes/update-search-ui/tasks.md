## 1. UI 重構
- [x] 1.1 將 `SearchBox` 重構為觸發按鈕與全螢幕模態視窗（Modal）。
- [x] 1.2 實作圓角膠囊狀（Subtle Pill）的觸發按鈕，Hover 時背景變色。
- [x] 1.3 移除按鈕右側的多餘快捷鍵圖示。
- [x] 1.4 調整 Modal 寬度為 `max-w-3xl`，並微調內部字體大小（輸入框 base，標題 15px）。

## 2. 邏輯與功能實作
- [x] 2.1 實作最近搜尋記錄（Recent Searches）：
    - 使用 `chrome.storage.local` 存取 `{ term, time }` 物件。
    - 紀錄上限 10 筆，並顯示相對時間（如 5m ago）。
- [x] 2.2 實作自動清空邏輯：關閉 Modal 時將 `q` 重設為空字串。
- [x] 2.3 快捷鍵監聽：實作 `Ctrl+F`、`Ctrl+K` 與 `/` 開啟彈窗。
- [x] 2.4 鍵盤導覽：實作方向鍵與 Enter 開啟結果。
- [x] 2.5 鍵鼠同步：確保滑鼠懸停與鍵盤選中狀態視覺一致（統一使用 `var(--card)` 背景）。
- [x] 2.6 搜尋過濾：過濾掉沒有 `subcategoryId` 的卡片項目。

## 3. 視覺與主題連動
- [x] 3.1 引入 `useOrganizations` 獲取顏色，套用於 Modal 計數文字與輸入框光標。
- [x] 3.2 在搜尋結果右側加入動作提示文字（↵ 開啟），使用 `var(--accent)`。
- [x] 3.3 優化 Favicon 顯示，移除多餘背景。
- [x] 3.4 實作卡片跳轉高亮：修復 class 衝突，確保完全使用組織代表色顯示 Outline 與 Box Shadow。
- [x] 3.5 移除分類標題下的分隔線，保持介面簡潔。