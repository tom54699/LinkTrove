# Tasks: Optimize Group Interactions

## Stage 1: GroupsView Refactoring
- [ ] 修改 `src/app/groups/GroupsView.tsx`:
  - [ ] Update `remove` function: Implement optimistic UI update (setGroups first).
  - [ ] Update Kebab Menu Button: Change color class to `text-[var(--accent)]` and enhance hover effect.

## Stage 2: Verification
- [ ] 測試刪除 Group，確認消失是否即時且流暢。
- [ ] 確認設定按鈕是否足夠顯眼。
